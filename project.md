Emese is an AI assistant who helps me (Mate) in my daily tasks or programming challanges, thinking, building, reminds me automatically and i can set reminders too. It is my own Jarvis who will improve from basic AI assistant into a whole Life OS. It is the 4th version of Emese, because my knowledge grew bigger I am restarting it from scratch and build it up into the Life OS

Rules for cursor agent:
- Constantly run tests and push to github
- if you are not .85 confident what to do next ask user before acting

Emese:
- Emese's description and promt is in Emese.md

Máté:
- The creater AKA me. My description is in Máté.md

Version 0.4.1

Frontend:
React.JS 
- we will create a React webapp as the first version that I can deploy to Vercel so I can run it on other devices as well. 

Frontend design
Home Page:
chat:
- on the right side will be the chat where user can enter any text and the assistant answers. when question asked display thinking animation in the chat box like in chatgpt. Once answer comes in from llm remove the thinking animation. it does not have to be read aloud so no tts on thinking...
toolbox:
- next to the chat box (where user enters input) add a toolbox clicking on the toolbox it should show 2 toogles (text to speach enable or disable, and mute) 
time and weather:
time and weather in location should be displayed on the bottom left side 
widgets:
widgets should be on the top left side, in this first version it will be my schedule. it can display the current and next event. This will be generated by the LLM(scheduling agent) based on the @scheduling_parameters.md. when clicked in the schedule widget make it open up the full schedule in a pop up window.

Other Pages:
studies widget:
- if asked to show me todays study the webapp will change from the home page to the study page. This will display a study choosen by the (study agent) where i can read about a current study to widen my knowledge. The agent will use the @studies_parameters.md. This page should be a nice place to read a study with a back to home arrow on the top left 

News Page:
- if asked to open News, open News Page. it should display the 10 articles for the day in an old news paper style. the instrucions are in @news_letter_parameters.md
Morning Page:
- between 5 and 6am every day in the widget section it shows 2 buttons next to each other they should fill out the full section of the widget section. button 1 meditation button 2 motivation

colors:
primary colors:
- light and 2003 style colors
background:
- the bg should be light white and light blue like a 2003 macbook
chat:
- in the chat the bg should be black and the users message bubble should be orange and the llm should not have a message box. 
dividers:
- the dividers are lines dividing the middle so chat on the right and widget and time,weather on the left. Also dividing the time,weather and widget horizontally 

object sizes:
- all the object sizes should use boostrap as well as the webapp it self so it can adjust to any screens.

Backend:
Python 
- the backend will be python. Python works well with ML and LLMs and it is purely LLM project since it is a smart AI assistant
backend structure:
- we are using sepparate agents for the tasks. emese will be the main agent choosing who to call based on the questions. the structure should be made in a way so it is easy to edit each agent so i can easily see what agent does what and add or remove agents. 
agent examples:
- Emese main agent it decides and this agent communicates with the user. It gives the orders and the final personality to the system
- Scheduling agent - creates the daily schedule. it knows everything about scheduling and user's schedule later will be responsible for editing schedules or any task with scheduling like setting up a meeting sending something on time etc..
- Study agent - helps user widen there knowledge, knows everything about learning and teaching
etc...
- it is true for the whole project it should be easy to wire in or out capebilities and functions since we will be editing and upgrading this project a lot
- make the folders in an organized way so it is easy to see what does what what is connected to what what is part of what
Storage:
local SQL Lite
- First we will build out the local storage structure because the AI assisntant wont be as big but later we will use a hybrid approach with supabase as cloud (forever storage) and local SQL Lite (only for 72 hours storage) 

Deployment:
- Vercel - frontend
- Railway - backend

APIs:
- LLM (Gemini, Claude later LLAMA as local and offline llm)
- voice processing whisperAPI
- weather and google searches, news, studies (Serper)
- TTS Elevenlabs 
- .env file already created with the API keys
capebilities:
Trigger Based:
- answer questions using LLM or if needed use Serper API
- open pages
- create notes as txt.file and interract with it like edit it read it aloud delete them or find them
- create reminnders and actually remind me with notification badges that pops up on the window. interact with them like edit them or delete them or find them


Automatic:
(everything it does automaticly it should display a message in the chat about so i can go back and check what it did)
- every morning at 4am it creates a schedule using the @scheduling_parameters.md - if done it should be displayed in the chat "schedule done!" or "schedule failed due to ..."
- every morning at 4:30am it creates a study using the @studies_parameters.md this is for me to widen my knowledge. the study agent should use google to find an interesting study recently published and llm should display it in a nice way to understand it not changing the study itself but add to it or explain complex things in it. if done it should be displayed in the chat "study created!" or "study creation failed due to ..."
- every midnight it should create a news letter using @news_letter_parameters.md - display if done "news letter created" or "news letter creation failed due to ..."

how it Emese works:
- Emese always listents in the background for the wakeword "computer" 
- if wakeword heard it listens for question or command and does it
- it also constantly checks for any reminders set by itself or any automatic action going off
- emese has voice and typing option as well
- use elevenlabs for tts and local as fallback 